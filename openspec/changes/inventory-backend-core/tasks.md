# Tasks: Inventory Backend Core

> Generated by `sdd-tasks` — breaks the spec and design into concrete implementation tasks.
> Total: 49 tasks across 6 phases | Est. ~7,565 lines | Budget risk: HIGH

---

## Phase 1 — Bootstrap & Auth Foundation (17 tasks)

### T-1.1: Project Configuration & Build Setup
- **Description:** Update `package.json` with all required deps (express, pg, jsonwebtoken, bcrypt, valibot, helmet, cors, express-rate-limit, morgan, multer, cookie-parser, vitest, supertest, biome, @types/*). Add all scripts (dev, build, start, db:migrate, db:seed, test, lint, format). Update `tsconfig.json` with path aliases. Create `.env.example`, `biome.json`, `vitest.config.ts`. Create empty `src/` directory structure per design. Create `tests/` directories.
- **Files:** `package.json` (modify), `tsconfig.json` (modify), `biome.json` (new), `.env.example` (new), `.env` (new), empty dirs
- **Deps:** None (foundation)
- **Est. lines:** ~70
- **AC:** REQ-1.1 — `bun install` succeeds, `bun run dev` starts on 4000, env vars loadable

### T-1.2: Shared Domain Error Classes
- **Description:** Abstract `AppError` base class with message, statusCode, code, details. Five subclasses: `NotFoundError` (404, NOT_FOUND), `ValidationError` (400, VALIDATION_ERROR), `UnauthorizedError` (401, UNAUTHORIZED), `ForbiddenError` (403, FORBIDDEN), `ConflictError` (409, CONFLICT). Each preserves prototype chain via `Object.setPrototypeOf` and captures stack trace.
- **Files:** `src/shared/errors/AppError.ts`, `NotFoundError.ts`, `ValidationError.ts`, `UnauthorizedError.ts`, `ForbiddenError.ts`, `ConflictError.ts`
- **Deps:** T-1.1
- **Est. lines:** ~70
- **AC:** REQ-1.12 — Errors map to correct HTTP codes, instanceof checks work, AppError cannot be instantiated directly

### T-1.3: Shared Types, Constants & Utilities
- **Description:** Express Request augmentation (`express.d.ts`) adding `user: { userId, email, role }`. Generic `PaginatedResult<T>` and `PaginationParams`. Role constants (`ADMIN`, `OPERADOR`, `SOLO_LECTURA`). Movement type string constants. `asyncHandler` wrapper for Express async routes. Pagination offset/limit helpers.
- **Files:** `src/shared/types/express.d.ts`, `pagination.ts`, `src/shared/constants/roles.ts`, `movement-types.ts`, `src/shared/utils/async-handler.ts`, `pagination.ts`
- **Deps:** T-1.1
- **Est. lines:** ~85
- **AC:** REQ-X.1 — req.user is typed, PaginatedResult compiles generically, asyncHandler catches errors and forwards to next()

### T-1.4: Database Connection Pool & Migration Runner
- **Description:** `connection.ts`: `createPool()` factory using `pg.Pool` with DATABASE_URL, max 20 connections, 30s idle timeout, 2s connection timeout, pool error handler. `closePool()` for graceful shutdown. `migrate.ts`: reads `.up.sql` from migrations directory, tracks applied in `schema_migrations` table, applies pending sequentially within transactions.
- **Files:** `src/infrastructure/database/postgres/connection.ts`, `migrate.ts`
- **Deps:** T-1.1
- **Est. lines:** ~85
- **AC:** REQ-1.2, REQ-6.11 — Pool connects with valid DATABASE_URL, migration runner applies SQL files in order, re-running skips already-applied

### T-1.5: SQL Migration 001 — Roles, Users & Refresh Tokens
- **Description:** Three sequential migrations with UP/DOWN: `001-roles` (id UUID PK, name VARCHAR UNIQUE, description TEXT). `002-users` (id UUID PK, email VARCHAR UNIQUE NOT NULL, password_hash VARCHAR NOT NULL, name VARCHAR NOT NULL, role_id UUID FK->roles, active BOOLEAN DEFAULT TRUE, created_at, updated_at). `003-refresh-tokens` (id UUID PK, user_id UUID FK->users, token_hash VARCHAR NOT NULL, expires_at TIMESTAMPTZ NOT NULL, revoked BOOLEAN DEFAULT FALSE, created_at). Indexes on users.email, refresh_tokens.user_id, refresh_tokens.revoked.
- **Files:** `migrations/001-roles.{up,down}.sql`, `002-users.{up,down}.sql`, `003-refresh-tokens.{up,down}.sql`
- **Deps:** T-1.4
- **Est. lines:** ~150
- **AC:** REQ-1.2 — All tables created with correct columns, FKs, indexes. Duplicate email rejected. Down migrations DROP all tables.

### T-1.6: Database Seed Script
- **Description:** `seed.ts` inserts 3 roles (admin, operador, solo_lectura) and default admin user (admin@sistema.com / Admin123! — bcrypt hashed). Uses ON CONFLICT DO NOTHING for idempotency. Script via `db:seed`.
- **Files:** `src/infrastructure/database/postgres/seeds/seed.ts`
- **Deps:** T-1.4, T-1.5
- **Est. lines:** ~65
- **AC:** REQ-1.3 — First run: 3 roles + 1 admin. Second run: no duplicates. Admin can authenticate.

### T-1.7: Domain Entities — User & Email Value Object
- **Description:** `Email` VO: private constructor, static `create(raw)` factory with regex validation, normalize (lowercase+trim), `equals()`, `toString()`. `User` entity: readonly id, email:Email, name, roleId, active, createdAt, updatedAt. `canAccess(allowedRoles)`, `deactivate()` returns new immutable User with active=false. Pure domain — zero framework imports.
- **Files:** `src/domain/value-objects/Email.ts`, `src/domain/entities/User.ts`
- **Deps:** T-1.2 (ValidationError)
- **Est. lines:** ~70
- **AC:** REQ-X.1 — Invalid email throws ValidationError, email normalized to lowercase, User immutable, domain has no infra imports

### T-1.8: Domain Repository Interface & Application Service Ports
- **Description:** `IUserRepository` interface: findById, findByEmail, findAll (paginated), save, updateRole, deactivate. Re-export from application/ports/repositories/. `IJwtService` port: generateAccessToken, generateRefreshToken, verifyAccessToken, verifyRefreshToken + JwtPayload type (userId, email, role). `IPasswordService` port: hash, verify.
- **Files:** `src/domain/repositories/IUserRepository.ts`, `src/application/ports/repositories/IUserRepository.ts` (re-export), `src/application/ports/services/IJwtService.ts`, `IPasswordService.ts`
- **Deps:** T-1.7
- **Est. lines:** ~50
- **AC:** REQ-X.1 — Domain interface has zero infra imports, ports are pure TypeScript contracts

### T-1.9: Application Auth Use Cases
- **Description:** `LoginUser`: validate credentials via PasswordService, check active, generate tokens via JwtService, store hashed refresh token in DB. `RefreshToken`: verify token, check DB for revocation, detect reuse (revoke ALL on reuse), rotate. `LogoutUser`: revoke refresh token in DB. Each: single `execute(input)` method, throws domain errors, no HTTP coupling.
- **Files:** `src/application/use-cases/auth/LoginUser.ts`, `RefreshToken.ts`, `LogoutUser.ts`
- **Deps:** T-1.8
- **Est. lines:** ~130
- **AC:** REQ-1.6, REQ-1.7, REQ-1.8 — Valid login returns tokens, invalid login throws generic 401, refresh rotates tokens, reuse attack detection revokes all, logout revokes

### T-1.10: Infrastructure Services — JwtService & PasswordService
- **Description:** `JwtService`: sign access tokens (15min, userId+email+role claims), sign refresh tokens (7d, userId only), verify with respective secrets. Config via constructor. `PasswordService`: bcrypt hash with 12 salt rounds, bcrypt.compare for verify.
- **Files:** `src/infrastructure/services/JwtService.ts`, `PasswordService.ts`
- **Deps:** T-1.8
- **Est. lines:** ~90
- **AC:** REQ-1.5 — Access tokens expire in 15min, refresh in 7d, expired tokens rejected on verify, hash+verify roundtrip works

### T-1.11: Infrastructure — PostgresUserRepository
- **Description:** Implements IUserRepository with pg Pool. findById: JOIN roles. findByEmail: WHERE email=$1. findAll: LIMIT/OFFSET pagination. save: INSERT RETURNING. updateRole/deactivate: UPDATE. Row mapper snake_case to camelCase. All queries parametrized — zero string concatenation for values.
- **Files:** `src/infrastructure/database/repositories/PostgresUserRepository.ts`
- **Deps:** T-1.4, T-1.7, T-1.8
- **Est. lines:** ~110
- **AC:** REQ-X.2 — findById returns User with role data, findByEmail(null) returns null, all queries use $N parameters

### T-1.12: Middleware — Auth (JWT Verify) & RBAC
- **Description:** `auth` middleware: extracts Bearer token, calls verifyAccessToken, attaches payload to req.user. Returns 401 UNAUTHORIZED or TOKEN_EXPIRED. `rbac(allowedRoles[])` factory: checks req.user.role against allowed list, returns 403 FORBIDDEN if denied, 401 if no user.
- **Files:** `src/infrastructure/http/middleware/auth.ts`, `rbac.ts`
- **Deps:** T-1.2, T-1.10, T-1.3
- **Est. lines:** ~60
- **AC:** REQ-1.9, REQ-1.10 — Valid token populates req.user, missing token returns 401, expired token returns 401, wrong role returns 403

### T-1.13: Middleware — Rate Limiter & Valibot Validator
- **Description:** Three rate limit tiers using express-rate-limit: public (20 req/15min), authenticated (1000 req/15min), import (10 req/15min). Returns 429 with Retry-After. `validate(schema)` middleware: valibot safeParse on req.body, returns 400 VALIDATION_ERROR with field-level issues on failure, replaces req.body with parsed output on success.
- **Files:** `src/infrastructure/http/middleware/rate-limiter.ts`, `validator.ts`
- **Deps:** T-1.2
- **Est. lines:** ~70
- **AC:** REQ-1.11, REQ-X.3 — 21st public request returns 429 with Retry-After, invalid input returns 400 with field errors

### T-1.14: Middleware — Error Handler, CORS & Helmet
- **Description:** `createErrorHandler`: maps AppError to HTTP responses with error code, stack traces in dev only, hides internals in production. `createCorsMiddleware`: CORS with CORS_ORIGIN env var whitelist, credentials. `createHelmetMiddleware`: standard security headers.
- **Files:** `src/infrastructure/http/middleware/error-handler.ts`, `cors.ts`, `helmet.ts`
- **Deps:** T-1.2
- **Est. lines:** ~75
- **AC:** REQ-1.4, REQ-1.12 — NotFoundError->404, ValidationError->400, UnauthorizedError->401, ForbiddenError->403, ConflictError->409, unhandled->500 (no stack in prod), Helmet headers present, CORS blocks unlisted origins

### T-1.15: Presentation — AuthController & Health/Auth Routes
- **Description:** `AuthController`: login (extract email/password, call LoginUser, set refresh cookie httpOnly Secure SameSite=Strict, return accessToken+user), refresh (read cookie, call RefreshToken, rotate cookie), logout (read cookie, call LogoutUser, clear cookie). `healthRouter`: GET /api/health returns `{ status, timestamp, uptime }`. `authRouter`: POST /login (public limiter + validate), POST /refresh (public limiter), POST /logout (auth). DTOs. Route aggregator.
- **Files:** `src/presentation/controllers/AuthController.ts`, `dtos/AuthDtos.ts`, `src/infrastructure/http/routes/health.routes.ts`, `auth.routes.ts`, `routes/index.ts`
- **Deps:** T-1.9, T-1.12, T-1.13, T-1.14
- **Est. lines:** ~190
- **AC:** REQ-1.4, REQ-1.6, REQ-1.7, REQ-1.8 — Login 200+token+Set-Cookie, invalid creds 401, refresh rotates cookie, logout clears cookie, health returns 200

### T-1.16: Infrastructure — DI Composition Root, Express App Factory & Entry Point
- **Description:** `AppDependencies` interface. `buildDependencies(pool)`: instantiates all singletons. `createApp(deps)`: Express factory with middleware chain (CORS->Helmet->body-parser->cookie-parser->Morgan), health routes before rate limiter, route mounting, 404 handler, global error handler. `src/index.ts`: create pool, build deps, create app, listen on PORT 4000, SIGTERM/SIGINT handlers for graceful shutdown (close pool, exit 0, force exit 1 after 30s).
- **Files:** `src/infrastructure/dependencies.ts`, `src/infrastructure/http/server.ts`, `src/index.ts` (modify)
- **Deps:** T-1.10, T-1.11, T-1.12, T-1.13, T-1.14, T-1.15
- **Est. lines:** ~175
- **AC:** REQ-1.1, REQ-6.12 — Server starts on 4000, all routes reachable, 404 for unknown routes, SIGTERM triggers graceful shutdown+exit 0

### T-1.17: Auth Integration & E2E Tests
- **Description:** Test helpers: test-pool (TEST_DATABASE_URL), seed-data (migrate+seed), auth-helpers (generate tokens). E2E auth tests with supertest: login valid, login wrong password (401 generic), login non-existent email (401), login missing fields (400), refresh with valid cookie, refresh no cookie (401), refresh revoked token reuse detection, logout clears cookie, health 200, protected route without token 401, RBAC 403.
- **Files:** `tests/e2e/auth.test.ts`, `tests/helpers/test-pool.ts`, `seed-data.ts`, `auth-helpers.ts`
- **Deps:** T-1.16
- **Est. lines:** ~275
- **AC:** REQ-1.6 through REQ-1.11, REQ-X.4 — All tests pass, login/refresh/logout flow works end-to-end, invalid credentials never reveal which field is wrong

---

## Phase 2 — Categories & Products (8 tasks)

### T-2.1: Migration 002 — Categories & Products Tables
- **Description:** `004-categories`: id UUID PK, name VARCHAR UNIQUE NOT NULL, description TEXT, created_at, updated_at. `005-products`: id UUID PK, name VARCHAR NOT NULL, reference VARCHAR UNIQUE NOT NULL, category_id UUID FK nullable, description TEXT, image_url TEXT, cost_price NUMERIC(12,2) NOT NULL, sale_price NUMERIC(12,2), stock INTEGER DEFAULT 0, min_stock INTEGER DEFAULT 0, max_stock INTEGER DEFAULT 999999, active BOOLEAN DEFAULT TRUE, created_by UUID FK->users, created_at, updated_at.
- **Files:** `migrations/004-categories.{up,down}.sql`, `005-products.{up,down}.sql`
- **Deps:** T-1.5 (users table), T-1.4
- **Est. lines:** ~100
- **AC:** REQ-2.1 — Tables with correct columns, reference UNIQUE, category_id nullable FK, active defaults TRUE, cost_price/sale_price NUMERIC(12,2)

### T-2.2: Value Objects — Money & StockLevel
- **Description:** `Money`: stores cents as integer, `fromDecimal(amount)` validates >= 0, `zero()`, `toDecimal()`, `add()`, `multiply()`, `equals()`. `StockLevel`: `create(min, max)` validates min>=0 and max>=min, `defaults()` returns (0, 999999), `isWithin(quantity)`.
- **Files:** `src/domain/value-objects/Money.ts`, `StockLevel.ts`
- **Deps:** T-1.2 (ValidationError)
- **Est. lines:** ~65
- **AC:** REQ-2.3 — Money(10.50).toDecimal()=10.50, Money(-1) throws, StockLevel(5,10) succeeds, StockLevel(10,5) throws

### T-2.3: Domain — Category Entity & Repository Interface
- **Description:** `Category`: readonly id, name, description, createdAt, updatedAt. `ICategoryRepository`: findById, findByName, findAll (ordered by name), save, update, delete.
- **Files:** `src/domain/entities/Category.ts`, `src/domain/repositories/ICategoryRepository.ts`
- **Deps:** T-1.1
- **Est. lines:** ~40
- **AC:** REQ-2.2 — Category entity compiles, interface has no infra imports

### T-2.4: Domain — Product Entity & Repository Interface
- **Description:** `Product`: readonly with costPrice:Money, salePrice:Money|null, stockLevel:StockLevel. Methods: isLowStock(), canSell(qty), recordEntry(qty) — validates positive+max, recordExit(qty) — validates positive+stock, both return new Product (immutable). `IProductRepository`: findById, findAll(filters,page,limit), findByReference, save, update, softDelete, findLowStock, updateStock(productId,newStock,client?).
- **Files:** `src/domain/entities/Product.ts`, `src/domain/repositories/IProductRepository.ts`
- **Deps:** T-2.2 (Money, StockLevel), T-1.2
- **Est. lines:** ~90
- **AC:** REQ-2.3 — recordEntry(+5) increases stock, recordExit(-5) decreases, exit with insufficient stock throws, isLowStock true when stock<=min

### T-2.5: Application — Category & Product Use Cases
- **Description:** Category: CreateCategory (unique name), ListCategories, GetCategory. Product: CreateProduct (unique reference, stock=0, active=true), GetProduct, ListProducts (search/filter/pagination), UpdateProduct (validate ref uniqueness on change), DeleteProduct (soft delete). All throw domain errors.
- **Files:** Categories: `src/application/use-cases/categories/CreateCategory.ts`, `ListCategories.ts`, `GetCategory.ts` + port re-export. Products: `CreateProduct.ts`, `GetProduct.ts`, `ListProducts.ts`, `UpdateProduct.ts`, `DeleteProduct.ts`
- **Deps:** T-2.3, T-2.4, T-1.8 (IUserRepository for created_by)
- **Est. lines:** ~280
- **AC:** REQ-2.2 through REQ-2.7 — Create product with valid data succeeds, duplicate ref throws ConflictError, minStock>maxStock throws ValidationError, list paginated, soft delete sets active=false

### T-2.6: Infrastructure — PostgresCategoryRepository & PostgresProductRepository
- **Description:** Category repo: name search, name-ordered list. Product repo: dynamic WHERE builder for filters (ILIKE search, category_id), findLowStock (WHERE active AND stock<=min_stock), updateStock with optional PoolClient for transaction support. Row mappers construct Money and StockLevel VOs. All queries parametrized.
- **Files:** `src/infrastructure/database/repositories/PostgresCategoryRepository.ts`, `PostgresProductRepository.ts`
- **Deps:** T-2.3, T-2.4, T-1.4
- **Est. lines:** ~250
- **AC:** REQ-2.2, REQ-2.3, REQ-2.4, REQ-X.2 — Category+product CRUD works, list filters by search term, findLowStock returns correct products, all queries parametrized

### T-2.7: Presentation — Category & Product Controllers, Routes & Valibot Schemas
- **Description:** `CategoryController`: list, getById, create, update. `ProductController`: create, getById, list, update, delete. Valibot schemas: `CreateProductSchema` (name 1-200 chars, reference 1-100 chars, costPrice>=0, salePrice>=0 nullable, minStock>=0, maxStock>=1), `UpdateProductSchema`, `ListProductsQuerySchema` (page, limit, search, categoryId). Route wiring with auth+RBAC: POST/PUT -> admin/operador, DELETE -> admin, GET -> public.
- **Files:** `src/presentation/controllers/CategoryController.ts`, `ProductController.ts`, `dtos/ProductDtos.ts`, `dtos/CategoryDtos.ts`, `src/infrastructure/http/routes/products.routes.ts`, `categories.routes.ts`
- **Deps:** T-2.5, T-1.12, T-1.13, T-1.16 (to wire routes)
- **Est. lines:** ~250
- **AC:** REQ-2.2 through REQ-2.7, REQ-X.3 — POST product 201, POST with missing name 400, GET paginated, GET /:id with category name, PUT updates, DELETE soft-deletes, non-admin cannot DELETE

### T-2.8: Tests — Category & Product E2E Tests
- **Description:** E2E tests with supertest: create category, list categories, create product, duplicate reference 409, invalid stock levels 400, list with pagination, list with search, get by ID (200+404), update, soft delete, verify deleted excluded from list, RBAC. Unit tests for Product entity (isLowStock, recordEntry, recordExit), Money, StockLevel VOs.
- **Files:** `tests/e2e/products.test.ts`, `tests/unit/domain/entities/Product.test.ts`, `tests/unit/domain/value-objects/Money.test.ts`, `StockLevel.test.ts`
- **Deps:** T-2.7, T-1.17 (test helpers)
- **Est. lines:** ~320
- **AC:** REQ-2.1 through REQ-2.7, REQ-X.4 — All E2E tests pass, unit tests cover entity invariants, soft-deleted products invisible

---

## Phase 3 — Stock & Movements (6 tasks)

### T-3.1: Migration 003 — Inventory Movements Table
- **Description:** `inventory_movements`: id UUID PK, product_id UUID FK->products, movement_type VARCHAR, quantity INTEGER CHECK(>0), reason TEXT, reference_doc VARCHAR, created_by UUID FK->users, created_at TIMESTAMPTZ. Indexes on product_id, movement_type, created_at, created_by.
- **Files:** `migrations/006-inventory-movements.{up,down}.sql`
- **Deps:** T-2.1 (products table), T-1.4
- **Est. lines:** ~50
- **AC:** REQ-3.1 — Table with all columns, FKs, indexes, positive quantity constraint, invalid FK rejected

### T-3.2: Domain — MovementType, InventoryMovement Entity & Repository Interface
- **Description:** `MovementType` VO: string enum with entry/exit type sets, `create(raw)`, `isEntry(type)`, `isExit(type)`. `InventoryMovement` entity: readonly fields, static factory methods `createEntry()` and `createExit()` that validate type-direction match. `IMovementRepository`: findById, findAll(filters,page,limit), save(movement,client?), getSummaryByType.
- **Files:** `src/domain/value-objects/MovementType.ts`, `src/domain/entities/InventoryMovement.ts`, `src/domain/repositories/IMovementRepository.ts`
- **Deps:** T-1.2
- **Est. lines:** ~80
- **AC:** REQ-3.1, REQ-X.1 — MovementType.create("purchase").isEntry()=true, create("sale").isExit()=true, Invalid type throws, createEntry with exit type throws

### T-3.3: Application — Stock & Movement Use Cases
- **Description:** `GetProductStock`: returns stock levels + isLowStock. `UpdateStockLevels`: validates min<max. `GetLowStockProducts`: queries stock<=min_stock. `RegisterEntry`: creates movement + increments stock in transaction. `RegisterExit`: decrements stock (or throws InsufficientStock). `GetMovement`, `ListMovements`: query with filters.
- **Files:** `src/application/use-cases/inventory/GetProductStock.ts`, `UpdateStockLevels.ts`, `GetLowStockProducts.ts`, `movements/RegisterEntry.ts`, `RegisterExit.ts`, `GetMovement.ts`, `ListMovements.ts`, `src/application/ports/repositories/IMovementRepository.ts` (re-export)
- **Deps:** T-3.2, T-2.4 (IProductRepository)
- **Est. lines:** ~250
- **AC:** REQ-3.2 through REQ-3.7 — RegisterEntry increments stock, RegisterExit decrements (or throws InsufficientStock), GetLowStockProducts returns only below-min

### T-3.4: Infrastructure — PostgresMovementRepository
- **Description:** Implements IMovementRepository with parametrized queries. `save` accepts optional PoolClient for transaction participation. `findAll` supports filters: movement_type, product_id, date_from, date_to, user_id. `getSummaryByType`: SELECT movement_type, COUNT(*), SUM(quantity) GROUP BY.
- **Files:** `src/infrastructure/database/repositories/PostgresMovementRepository.ts`
- **Deps:** T-3.2, T-1.4
- **Est. lines:** ~150
- **AC:** REQ-3.5, REQ-3.6, REQ-3.7 — Movement save + stock update in same transaction, concurrent updates safe, filtered list queries correct, summary by type aggregated

### T-3.5: Presentation — Movement Controller, Routes & Valibot Schemas
- **Description:** `MovementController`: getStock, updateLevels, getLowStock, registerEntry, registerExit, getMovement, listMovements. Valibot schemas for entry/exit (productId UUID, quantity positive int, type valid enum). Routes: GET /api/products/:id/stock, PUT .../stock/levels, GET /api/products/low-stock, POST /api/movements/entry, POST /api/movements/exit, GET /api/movements, GET /api/movements/:id.
- **Files:** `src/presentation/controllers/MovementController.ts`, `dtos/MovementDtos.ts`, `src/infrastructure/http/routes/movements.routes.ts`
- **Deps:** T-3.3, T-1.12, T-1.13, T-1.16
- **Est. lines:** ~180
- **AC:** REQ-3.2 through REQ-3.7, REQ-X.3 — Entry increments stock, exit with insufficient stock 400, low-stock returns correct products, movements list filters

### T-3.6: Tests — Movement E2E Tests
- **Description:** E2E: entry verifies stock increase, exit verifies stock decrease, exit insufficient 400, get stock levels, update levels validates, low stock returns correct, movement list filters. Unit: InventoryMovement entity factories, MovementType VO validation.
- **Files:** `tests/e2e/movements.test.ts`, `tests/unit/domain/entities/InventoryMovement.test.ts`, `tests/unit/domain/value-objects/MovementType.test.ts`
- **Deps:** T-3.5, T-1.17
- **Est. lines:** ~250
- **AC:** REQ-3.1 through REQ-3.7, REQ-X.4 — All E2E pass, race condition test verifies transactional safety

---

## Phase 4 — Suppliers & Purchases (6 tasks)

### T-4.1: Migration 004 — Suppliers, Purchase Orders & Order Items
- **Description:** `suppliers`: id UUID PK, name VARCHAR NOT NULL, contact_name, email, phone, address, payment_terms, notes, active BOOLEAN DEFAULT TRUE, created_at, updated_at. `purchase_orders`: id UUID PK, supplier_id FK->suppliers, status VARCHAR DEFAULT 'pending', notes TEXT, total NUMERIC(12,2), created_by FK->users, created_at, updated_at. `purchase_order_items`: id UUID PK, purchase_order_id FK->purchase_orders ON DELETE CASCADE, product_id FK->products, quantity INTEGER, unit_price NUMERIC(12,2), received_quantity INTEGER DEFAULT 0.
- **Files:** `migrations/007-suppliers.{up,down}.sql`, `008-purchase-orders.{up,down}.sql`
- **Deps:** T-2.1 (products), T-1.5 (users), T-1.4
- **Est. lines:** ~100
- **AC:** REQ-4.1 — All 3 tables created, purchase_order_items has ON DELETE CASCADE, FKs enforced

### T-4.2: Domain — Supplier, PurchaseOrder, PurchaseOrderItem Entities & Repository Interfaces
- **Description:** `Supplier`: readonly fields. `PurchaseOrderItem`: subtotal() method. `PurchaseOrder`: canTransitionTo(newStatus) state machine (pending->ordered/cancelled, ordered->received/cancelled, received/cancelled=terminal), transitionTo() validates+returns new instance, calculateTotal(). `ISupplierRepository`, `IPurchaseOrderRepository` (with filters).
- **Files:** `src/domain/entities/Supplier.ts`, `PurchaseOrder.ts`, `PurchaseOrderItem.ts`, `src/domain/repositories/ISupplierRepository.ts`, `IPurchaseOrderRepository.ts`
- **Deps:** T-2.2 (Money), T-1.2
- **Est. lines:** ~140
- **AC:** REQ-4.2 through REQ-4.4 — canTransitionTo("ordered") from pending=true, from pending to "received"=false, transitionTo throws on invalid, subtotal = qty x unitPrice

### T-4.3: Application — Supplier & Purchase Use Cases
- **Description:** Supplier: CreateSupplier (admin), GetSupplier, ListSuppliers (paginated, active), UpdateSupplier. Purchase: CreatePurchaseOrder (admin, validates supplier+products, calculates total), GetPurchaseOrder (with items+product names), ListPurchaseOrders (status/supplier/date filters), UpdatePurchaseOrderStatus (validates transition), ReceivePurchaseOrder (transition to received, create InventoryMovements for each item via IMovementRepository, DB transaction).
- **Files:** Use cases under `suppliers/` and `purchases/` + port re-exports
- **Deps:** T-4.2, T-3.2 (IMovementRepository for goods reception)
- **Est. lines:** ~350
- **AC:** REQ-4.2 through REQ-4.6 — CreateSupplier creates active supplier, CreatePurchaseOrder with invalid product 400, status transitions validated, ReceivePurchaseOrder creates movements+updates stock atomically, non-admin users 403

### T-4.4: Infrastructure — PostgresSupplierRepository & PostgresPurchaseOrderRepository
- **Description:** Supplier repo: soft-delete filter active=TRUE. Purchase order repo: save inserts order+items in transaction, findById JOINs items+product names, findAll supports status/supplierId/date filters, updateStatus. All parametrized queries.
- **Files:** `src/infrastructure/database/repositories/PostgresSupplierRepository.ts`, `PostgresPurchaseOrderRepository.ts`
- **Deps:** T-4.2, T-1.4
- **Est. lines:** ~220
- **AC:** REQ-4.2, REQ-4.3, REQ-4.6, REQ-X.2 — Supplier CRUD works, order creation succeeds in transaction, order detail includes items+names, list filters by status

### T-4.5: Presentation — Supplier & Purchase Controllers, Routes & Valibot Schemas
- **Description:** `SupplierController`: create, getById, list, update. `PurchaseController`: create, getById, list, updateStatus, receive. Valibot schemas for all write operations. Routes at /api/suppliers and /api/purchases with auth+RBAC (admin for create/update, admin/operador for list/get).
- **Files:** `src/presentation/controllers/SupplierController.ts`, `PurchaseController.ts`, dtos, `routes/suppliers.routes.ts`, `purchases.routes.ts`
- **Deps:** T-4.3, T-1.12, T-1.13, T-1.16
- **Est. lines:** ~250
- **AC:** REQ-4.2 through REQ-4.6, REQ-X.3 — POST supplier 201, non-admin 403, POST purchase creates with correct total, PUT status validates transitions, POST receive atomically receives goods

### T-4.6: Tests — Supplier & Purchase E2E Tests
- **Description:** E2E: supplier CRUD lifecycle, purchase creation, status transitions (valid+invalid), goods reception creates movements+updates stock, reception with invalid transition 400, list filters. Unit: PurchaseOrder state machine, PurchaseOrderItem subtotal.
- **Files:** `tests/e2e/suppliers.test.ts`, `purchases.test.ts`, `tests/unit/domain/entities/PurchaseOrder.test.ts`, `Supplier.test.ts`
- **Deps:** T-4.5, T-1.17
- **Est. lines:** ~300
- **AC:** REQ-4.1 through REQ-4.6, REQ-X.4 — All E2E pass, state transitions match spec, goods reception creates movements, transaction rollback on failure

---

## Phase 5 — Reports (5 tasks)

### T-5.1: Application — Report Use Cases
- **Description:** `GetInventoryValue`: SUM(cost_price x stock) across active products, grouped by category. `GetProductRotation`: ranks products by movement volume within date range, split by in/out. `GetShrinkageReport`: aggregates shrinkage+adjustment_out movements by product, calculates value loss. `GetMovementsSummary`: groups by movement_type with count+total quantity. Pure domain data returns.
- **Files:** `src/application/use-cases/reports/GetInventoryValue.ts`, `GetProductRotation.ts`, `GetShrinkageReport.ts`, `GetMovementsSummary.ts`
- **Deps:** T-2.4 (IProductRepository), T-3.2 (IMovementRepository)
- **Est. lines:** ~200
- **AC:** REQ-5.1 through REQ-5.4 — Inventory value returns total+byCategory, rotation ranks by volume, shrinkage shows quantity+value loss, summary has counts per type, soft-deleted excluded

### T-5.2: Infrastructure — Report Query Implementations
- **Description:** Inventory value: LEFT JOIN categories, WHERE active=TRUE. Product rotation: SUM with CASE for in/out, GROUP BY, ORDER BY total DESC. Shrinkage: filtered to shrinkage/adjustment_out. Summary: GROUP BY movement_type. All parametrized.
- **Files:** Modify `PostgresProductRepository.ts` and `PostgresMovementRepository.ts` or create dedicated report file
- **Deps:** T-5.1
- **Est. lines:** ~120
- **AC:** REQ-5.1 through REQ-5.4 — Correct aggregates, soft-deleted excluded, date range filters applied, all queries parametrized

### T-5.3: Presentation — Report Controller & Routes
- **Description:** `ReportController`: getInventoryValue, getProductRotation, getShrinkage, getMovementsSummary. Routes at /api/reports/* with auth+admin/operador. Valibot schemas for date range (required for rotation/shrinkage).
- **Files:** `src/presentation/controllers/ReportController.ts`, `dtos/ReportDtos.ts`, `src/infrastructure/http/routes/reports.routes.ts`
- **Deps:** T-5.1, T-1.12, T-1.13, T-1.16
- **Est. lines:** ~120
- **AC:** REQ-5.1 through REQ-5.4 — Inventory value returns total+byCategory, product rotation requires date range (400 missing), shrinkage returns per-product

### T-5.4: Report Export Functionality (CSV)
- **Description:** `ExportReport` use case: accepts report type + format (csv), generates CSV string. Controller endpoint GET /api/reports/export/:type?format=csv sets Content-Type: text/csv and Content-Disposition: attachment. All 4 report types supported.
- **Files:** `src/application/use-cases/reports/ExportReport.ts`, `src/infrastructure/services/CsvExportService.ts`
- **Deps:** T-5.1, T-5.3
- **Est. lines:** ~140
- **AC:** REQ-5.5 — CSV returns valid headers+data, Content-Type text/csv, unsupported format 400, unsupported type 400

### T-5.5: Tests — Report E2E Tests
- **Description:** E2E tests for all 4 report endpoints with seeded data. Verify correct calculations. CSV export returns correct content. Missing date range 400. Soft-deleted exclusion.
- **Files:** `tests/e2e/reports.test.ts`, `tests/unit/application/reports/GetInventoryValue.test.ts`
- **Deps:** T-5.3, T-5.4, T-1.17
- **Est. lines:** ~250
- **AC:** REQ-5.1 through REQ-5.5, REQ-X.4 — All tests pass, calculations match expected, CSV has correct headers+data

---

## Phase 6 — Users & Polish (7 tasks)

### T-6.1: Infrastructure — File Storage Service & Multer Upload Middleware
- **Description:** `FileStorageService` implementing IFileStorageService: save(file,destination) saves to disk returns URL, delete(filePath) removes file. `createUploadMiddleware`: Multer with disk storage, uuid filenames, 5MB limit, allowed MIME types (jpeg, png, webp). Creates uploads/ on first use.
- **Files:** `src/infrastructure/services/FileStorageService.ts`, `src/application/ports/services/IFileStorageService.ts`, `src/infrastructure/http/middleware/upload.ts`
- **Deps:** T-1.1, T-1.2
- **Est. lines:** ~80
- **AC:** REQ-6.1, REQ-6.2 — File saves to disk, large files rejected, invalid types rejected, uploads/ created if missing

### T-6.2: Application — User CRUD Use Cases
- **Description:** `ListUsers` (paginated, includes role name, no password), `GetUser`, `CreateUser` (validates email uniqueness, password>=8 chars, hashes password), `UpdateUser` (name, email — validates uniqueness), `UpdateUserRole` (validates role exists, cannot change own), `DeactivateUser` (soft delete, cannot deactivate own, deactivated users rejected on login).
- **Files:** `src/application/use-cases/users/ListUsers.ts`, `GetUser.ts`, `CreateUser.ts`, `UpdateUser.ts`, `UpdateUserRole.ts`, `DeactivateUser.ts`
- **Deps:** T-1.8 (IUserRepository, IPasswordService)
- **Est. lines:** ~250
- **AC:** REQ-6.6 through REQ-6.10 — CreateUser with short password throws, duplicate email 409, ListUsers no password, UpdateUserRole own ID throws, DeactivateUser own ID throws, deactivated login rejected

### T-6.3: Application — Product Image & CSV Import/Export Use Cases
- **Description:** `UploadProductImage`: validates product exists, saves file, updates imageUrl. `DeleteProductImage`: deletes file, clears URL. `ImportProducts`: reads CSV, validates each row (creates category by name if needed), creates all in transaction (rollback on error), returns {imported, errors}. `ExportProducts`: queries active products, generates CSV.
- **Files:** `src/application/use-cases/products/UploadProductImage.ts`, `DeleteProductImage.ts`, `ImportProducts.ts`, `ExportProducts.ts`
- **Deps:** T-2.4 (IProductRepository), T-2.3 (ICategoryRepository), T-6.1 (IFileStorageService)
- **Est. lines:** ~250
- **AC:** REQ-6.2 through REQ-6.5 — Upload saves file+updates product, Delete clears file+URL, Import creates from valid CSV (rollback on error), Export returns active products CSV

### T-6.4: Presentation — User Controller, Routes & Valibot Schemas
- **Description:** `UserController`: list, getById, create, update, updateRole, deactivate. Valibot schemas: CreateUserSchema (email, password min 8, name, roleId), UpdateUserSchema, UpdateRoleSchema. Routes at /api/users with auth+admin-only RBAC.
- **Files:** `src/presentation/controllers/UserController.ts`, `dtos/UserDtos.ts`, `src/infrastructure/http/routes/users.routes.ts`
- **Deps:** T-6.2, T-1.12, T-1.13, T-1.16
- **Est. lines:** ~160
- **AC:** REQ-6.6 through REQ-6.10, REQ-X.3 — GET users returns paginated no-password, POST creates 201, duplicate email 409, role change works, own role change 400, deactivate sets active=false

### T-6.5: Presentation — Product Image, Import & Export Route Wiring
- **Description:** Add to ProductController: uploadImage, deleteImage, importProducts, exportProducts. Routes: POST /api/products/:id/image (auth+rbac+upload.single), DELETE /api/products/:id/image (auth+rbac), POST /api/products/import (auth+rbac admin+import limiter+upload.single), GET /api/products/export (auth).
- **Files:** Modify `ProductController.ts`, `products.routes.ts`
- **Deps:** T-6.3, T-1.12, T-1.13, T-6.1
- **Est. lines:** ~150
- **AC:** REQ-6.2 through REQ-6.5 — Image upload returns imageUrl, delete clears, import with valid CSV creates, invalid CSV rolls back, export returns CSV

### T-6.6: Health Readiness & Graceful Shutdown Completion
- **Description:** GET /api/health/ready: pings DB (SELECT 1), returns 200 if connected, 503 if not. SIGTERM/SIGINT: log shutdown, stop server (server.close), set 30s timeout, close pool, exit 0. On timeout: exit 1.
- **Files:** Modify `health.routes.ts` (add /ready), modify `src/index.ts` (full shutdown)
- **Deps:** T-1.4 (pool), T-1.16 (server)
- **Est. lines:** ~60
- **AC:** REQ-6.11, REQ-6.12 — Health ready 200 when DB up, 503 when down, SIGTERM triggers graceful shutdown, exit 0 clean, exit 1 on timeout

### T-6.7: Tests — User CRUD, Image Upload & CSV E2E Tests
- **Description:** E2E: user CRUD lifecycle, role change, self-role-change 400, self-deactivation 400, deactivated user login rejected, image upload+delete, CSV import valid, CSV import invalid rollback, CSV export. Unit: CreateUser use case, ImportProducts use case.
- **Files:** `tests/e2e/users.test.ts`, `products-import.test.ts`, `tests/unit/application/users/CreateUser.test.ts`, `products/ImportProducts.test.ts`
- **Deps:** T-6.4, T-6.5, T-6.6, T-1.17
- **Est. lines:** ~350
- **AC:** REQ-6.1 through REQ-6.12, REQ-X.4 — All tests pass, user lifecycle works, image+CSV operations work, deactivated login rejected

---

## Dependency Graph Summary

```
T-1.1 (foundation)
  +-> T-1.2 (errors) -> T-1.3 (types/constants)
  +-> T-1.4 (DB pool + migration runner) -> T-1.5 (SQL migrations) -> T-1.6 (seed)
  +-> T-1.2 -> T-1.7 (User entity) -> T-1.8 (ports) -> T-1.9 (auth use cases) -> T-1.15 (controller + auth routes)
  |                                                              |
  |                                     T-1.10 (JWT+Password) -> T-1.12 (auth middleware)
  |                                     T-1.11 (PG user repo) -> T-1.9
  |                                                              |
  |                            T-1.13 (rate-limit+validator)  T-1.14 (error handler+CORS+helmet)
  |                                                              |
  |                                     T-1.16 (DI + server + entry point)
  |                                          |
  |                                     T-1.17 (auth tests)
  |
  +-> T-2.1 (migration 002) -> T-2.2 (Money, StockLevel) -> T-2.4 (Product entity)
  |   +-> T-2.3 (Category entity) -> T-2.5 (use cases) -> T-2.7 (controllers+routes)
  |   |                                                    |
  |   +-> T-2.6 (PG repos) -> T-2.5                       T-2.8 (tests)
  |
  +-> T-2.1 -> T-3.1 (migration 003) -> T-3.2 (Movement entity) -> T-3.3 (use cases)
  |   +-> T-3.4 (PG movement repo) -> T-3.3 -> T-3.5 (controller) -> T-3.6 (tests)
  |
  +-> T-2.1 -> T-4.1 (migration 004) -> T-4.2 (Supplier+Purchase entities) -> T-4.3 (use cases)
  |   +-> T-4.4 (PG repos) -> T-4.3 -> T-4.5 (controller) -> T-4.6 (tests)
  |
  +-> T-2.4 + T-3.2 -> T-5.1 (report use cases) -> T-5.2 (queries) -> T-5.3 (controller)
  |   +-> T-5.4 (export) -> T-5.5 (tests)
  |
  +-> T-1.8 + T-2.4 -> T-6.2 (user CRUD) -> T-6.4 (user controller)
      T-6.1 (file storage) -> T-6.3 (image+CSV) -> T-6.5 (product extensions)
      T-6.6 (readiness+shutdown)
      -> T-6.7 (tests)
```

---

## Review Workload Forecast

### Line Estimates Summary

| Phase | Tasks | Est. New Lines |
|-------|-------|----------------|
| Phase 1: Bootstrap & Auth | 17 | ~1,800 |
| Phase 2: Categories & Products | 8 | ~1,395 |
| Phase 3: Stock & Movements | 6 | ~960 |
| Phase 4: Suppliers & Purchases | 6 | ~1,360 |
| Phase 5: Reports | 5 | ~830 |
| Phase 6: Users & Polish | 7 | ~1,300 |
| **Total** | **49** | **~7,645** |

### Forecast Metrics

1. **Total estimated lines changed:** ~7,645 across all phases
2. **Phase 1 estimate:** ~1,800 lines
3. **Chained PRs recommended: YES** — total far exceeds 400; Phase 1 alone is 4.5x the budget
4. **400-line budget risk: HIGH** — every individual phase exceeds 400 lines
5. **Decision needed before apply: YES** — orchestrator must decide chain strategy

### Recommended Chained PR Strategy

**Option A — Stacked PRs to main (for Phase 1):**
- PR #1: T-1.1 to T-1.6 (project config + DB schema + seed) — ~440 lines
- PR #2: T-1.7 to T-1.11 (domain + ports + services + repo) — ~380 lines
- PR #3: T-1.12 to T-1.16 (middleware + controllers + DI + server) — ~575 lines *(may need further split)*
- PR #4: T-1.17 (auth tests) — ~275 lines

**Option B — Feature Branch Chain (whole project):**
- Tracker PR (draft/no-merge)
- PR #1: Phase 1 foundation -> tracker branch
- PR #2: Phase 2 categories+products -> P1 branch
- PR #3: Phase 3 stock+movements -> P2 branch
- ...continuing through Phase 6

**Recommendation:** Option A for Phase 1 (independent merges, faster feedback), then reassess for subsequent phases.
